#!/usr/bin/env bash
# full-clone.sh — Clone Server A -> Server B with rsync
# Optimized for >=1Gbps, supports SSH key or password
set -euo pipefail
echo "=== Server Migration (rsync clone) ==="
# (Full script content from our fixed version goes here)
